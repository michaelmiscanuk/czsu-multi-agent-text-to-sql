[tool.poetry]
name = "czsu-multi-agent-app"
version = "0.1.0"
description = "A prototype project for LangGraph testing."
authors = ["Your Name <your.email@example.com>"]

packages = [
    { include = "CZSU_Multi_Agent_App" }
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
